import { Dancing_Script, Poppins, Rubik } from "next/font/google";

const rubik = Rubik({ subsets: ["latin"] });
const dancingScript = Dancing_Script({ subsets: ["latin"], weight: "600" });

export { rubik, dancingScript };
